apiVersion: 1
providers:
  - name: OPA Guard
    folder: OPA Policy Performance Guard
    type: file
    disableDeletion: true
    editable: false
    options:
      path: /var/lib/grafana/dashboards
