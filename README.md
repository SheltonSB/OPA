# OPA
OPA Policy Performance Guard is a developer tool that detects whether policy changes increase authorization latency. It benchmarks the main branch against each pull request, compares latency and memory usage, verifies correctness, and fails CI when performance regressions exceed configurable limits.
